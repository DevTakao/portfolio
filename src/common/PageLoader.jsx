const PageLoader = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center text-center bg-light">
      <h1 className="animate-pulse text-3xl font-medium">Loading...</h1>
    </div>
  );
};

export default PageLoader;
